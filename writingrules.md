

# VibePaper GUI 写作规则指南

本文档用于指导用户和 AI Agent 使用 VibePaper GUI 进行论文写作。

## 项目概述

VibePaper GUI 是一个基于树状结构的论文写作辅助工具，采用自顶向下的方式组织论文结构。论文以 Markdown 文件形式存储，通过标题层级组织结构，每个节点可包含描述、主题句、支撑句和图片等内容。

## Markdown 文件结构

### 标题层级规则

| 层级 | Markdown 语法 | 用途 | 是否可修改 |
|------|---------------|------|------------|
| 1级 | `#` | 论文总标题 | 可编辑 |
| 2-5级 | `##` ~ `#####` | 组织论文结构框架 | 不可修改 |
| 6级 | `######` | 具体段落内容 | 可撰写正文 |

**重要原则**：
- 1-5 级标题仅用于组织论文结构，不直接撰写正文内容
- 6 级标题代表一个具体段落，标题是主题句，正文是支撑句

### 节点元数据

每个标题节点下方可包含以下 HTML 注释形式的元数据：

```markdown
## 节点标题
<!-- editable: true -->
<!-- writable: true -->
<!-- expandable: true -->
<!-- description: 这里是节点的描述内容，用于指导如何撰写该部分 -->
```

#### 元数据字段说明

| 字段 | 类型 | 默认值 | 含义 |
|------|------|--------|------|
| `editable` | boolean | true | 标题是否可编辑 |
| `writable` | boolean | true | 是否可撰写正文内容（主题句、支撑句、图片） |
| `expandable` | boolean | true | 是否可添加子节点 |
| `description` | string | "" | 节点描述，用于指导该部分应如何撰写 |

**示例**：

```markdown
## 问题定义
<!-- editable: false -->
<!-- writable: false -->
<!-- expandable: false -->
<!-- description: 明确界定研究所要解决的问题及其背景。需要回答：问题是什么？为什么值得研究？有哪些实际应用场景？ -->
```

## 写作规则

### 基本原则

1. **保持结构完整**：不要修改 2-5 级标题，它们定义了论文的整体框架
2. **遵循描述指导**：每个节点的 `description` 字段提供了该部分的写作指导，请仔细阅读并遵循
3. **段落写作**：6 级标题代表一个段落，标题是主题句，正文是支撑句
4. **字数限制**：
   - 主题句：最多 50 字
   - 支撑句：最多 500 字

### 内容撰写规范

#### 主题句（Topic Sentence）

- **作用**：概括段落核心观点，作为段落的"标题"
- **要求**：简洁明了，一般为一句话
- **限制**：不超过 50 字
- **示例**：
  - ✅ "传统入侵检测系统对 APT 攻击的检测率不足 30%"
  - ❌ "我们发现传统的入侵检测系统在检测高级持续性威胁方面存在一些问题，检测率比较低"（过于冗长）

#### 支撑句（Supporting Sentences）

- **作用**：展开论述主题句的观点
- **内容**：可包含具体例子、数据、论证、引用
- **格式**：
  - 支持 LaTeX 公式：
    - 行内公式：`$E=mc^2$` → 显示为 $E=mc^2$
    - 块级公式：`$$\int_0^1 x^2 dx$$` → 独立成行显示
- **限制**：不超过 500 字
- **示例**：
  ```
  根据 Mandiant 2023 年报告，APT 攻击平均潜伏期为 146 天，传统 IDS 的检测率仅为 28%。
  主要原因在于：(1)孤立检测忽略攻击阶段关联；(2)签名匹配无法识别零日攻击；(3)误报率
  高达 90%，真实告警被淹没。
  ```

#### 图片

- **格式**：JPG、PNG、GIF
- **大小**：最大 5MB
- **用途**：展示架构图、流程图、实验结果、对比图表等
- **建议**：图片应配有文字说明，解释图片的核心信息

#### 子节点扩展

当节点标题以数字结尾时（如"影响场景1"、"技术挑战1"），表示该节点可以复制扩展：

- **扩展方式**：完整复制该节点及其所有子节点的结构
- **命名规则**：递增数字编号（如"影响场景1" → "影响场景2"）
- **注意事项**：
  - 必须完整复制整个子树结构，包括所有元数据
  - 不要破坏原有的层级关系
  - 新节点的元数据应与原节点保持一致

**示例**：

```markdown
<!-- 原节点 -->
#### 影响场景1
<!-- editable: false -->
<!-- writable: true -->
<!-- expandable: false -->
<!-- description: 描述问题在某个具体场景中的关键作用 -->

<!-- 扩展后的新节点 -->
#### 影响场景2
<!-- editable: false -->
<!-- writable: true -->
<!-- expandable: false -->
<!-- description: 描述问题在某个具体场景中的关键作用 -->
```

#### 内容段落添加

只有 `writable: true` 的节点才能添加内容段落，且必须遵循以下规则：

- **前提条件**：父节点的 `writable` 属性必须为 `true`
- **段落形式**：内容段落必须以 **6 级标题（######）** 形式添加
- **标题作用**：6 级标题的文本即为该段落的**主题句**
- **正文内容**：支撑句写入节点的 `content` 字段

**示例**：

```markdown
## 问题定义
<!-- editable: false -->
<!-- writable: true -->
<!-- expandable: false -->
<!-- description: 明确界定研究所要解决的问题 -->

<!-- 以下是添加的内容段落（6级标题） -->
###### 传统入侵检测系统对APT攻击检测率不足30%
<!-- editable: false -->
<!-- writable: true -->
<!-- expandable: false -->
<!-- description: 具体阐述检测率低的原因和数据支撑 -->
（支撑句内容写入 content 字段）
```

**错误示例**：

```markdown
<!-- ❌ 错误：在 writable: false 的节点下添加内容段落 -->
## 现有方法局限
<!-- writable: false -->
###### 传统方法存在局限
（不允许添加，因为父节点 writable: false）

<!-- ❌ 错误：使用非6级标题作为内容段落 -->
## 问题定义
<!-- writable: true -->
### 问题描述
（不允许，应使用6级标题 ######）
```

## AI Agent 使用指南

### 角色定位

Agent 应作为专业的写作助手，帮助用户：

1. **理解框架**：解析论文结构，理解各部分的作用和关联
2. **内容撰写**：根据 `description` 指导撰写高质量的主题句和支撑句
3. **逻辑检查**：确保各部分内容的一致性和逻辑连贯性
4. **扩展建议**：根据论文主题，建议需要扩展的节点

### 操作规范

#### 必须遵守

1. **读取结构**：首先读取 Markdown 文件，理解整体框架
2. **遵循描述**：严格按照 `description` 字段的指导撰写内容
3. **保持层级**：绝对不要修改 2-5 级标题结构
4. **字数限制**：主题句 ≤ 50 字，支撑句 ≤ 500 字

#### 数据存储

| 内容类型 | 存储字段 | 说明 |
|----------|----------|------|
| 节点标题 | `title` | Markdown 标题文本 |
| 主题句 | `topicSentence` | 6 级标题的标题文本 |
| 支撑句 | `content` | 段落正文内容 |
| 图片 | `imageUrl` | Base64 编码的图片数据 |
| 描述 | `description` | 元数据中的指导内容 |

### 写作流程建议

按照以下顺序逐步完善论文：

1. **核心 Insight**：理解论文的核心贡献和创新点
2. **问题定义**：明确研究问题、重要性和应用场景
3. **现状分析**：梳理现有方法及其局限性
4. **方法设计**：阐述解决方案的技术细节和创新性
5. **实验验证**：设计实验方案，验证方法有效性
6. **结果分析**：解释实验结果，讨论意义和启示

### 质量检查清单

撰写完成后，Agent 应检查：

- [ ] 所有 `writable: true` 的节点是否都有内容
- [ ] 主题句是否简洁且概括核心观点
- [ ] 支撑句是否有具体的例子、数据或论证
- [ ] 各部分内容是否逻辑连贯
- [ ] 是否有需要扩展的数字编号节点

## 用户使用指南

### 界面说明

```
┌─────────────────────────────────────────────────────────────────┐
│                        VibePaper GUI                            │
├──────────────┬──────────────────────────┬───────────────────────┤
│   树视图      │        编辑器             │      AI 面板          │
│  (大纲结构)   │    (内容编辑)             │    (对话助手)         │
│              │                          │                       │
│  📄 论文题目  │  标题: [可编辑]           │  💬 与 AI 对话        │
│  📂 Insight  │  描述: [指导文本]         │                       │
│    └─ 描述   │  主题句: [输入框]         │  🔧 AI 功能:          │
│  📂 问题定义 │  支撑句: [文本框]         │  - 生成子论点         │
│    └─ ...   │  图片: [上传区域]         │  - 扩展内容           │
│              │                          │  - 优化表达           │
│  宽度: 320px │  自适应宽度               │  宽度: 384px          │
└──────────────┴──────────────────────────┴───────────────────────┘
```

### 节点属性指示器

节点旁的彩色圆点表示该节点的属性：

| 指示器 | 颜色 | 属性 | 含义 |
|--------|------|------|------|
| 🔵 | 蓝色 | `editable` | 标题可编辑 |
| 🟢 | 绿色 | `writable` | 可撰写正文内容 |
| 🟠 | 橙色 | `expandable` | 可添加子节点 |

**灰色圆点**表示该属性为 `false`，对应功能不可用。

### 操作说明

| 操作 | 方法 | 条件 |
|------|------|------|
| 选择节点 | 点击树视图中的节点 | - |
| 编辑标题 | 点击编辑按钮 ✏️ 或双击节点 | `editable: true` |
| 添加子节点 | 点击添加按钮 ➕ | `expandable: true` |
| 删除节点 | 点击删除按钮 🗑️ | - |
| 撰写内容 | 在编辑器中填写主题句和支撑句 | `writable: true` |
| 上传图片 | 点击图片上传区域 | `writable: true` |
| 展开/折叠 | 点击节点前的箭头 ▶ | 有子节点 |

### 文件操作

| 操作 | 功能 | 快捷键 |
|------|------|--------|
| 导出 Markdown | 导出完整的 .md 文件 | - |
| 导入 Markdown | 导入已有的 .md 文件 | - |
| 保存项目 | 保存为 .json 项目文件 | Ctrl+S |
| 加载项目 | 加载 .json 项目文件 | - |
| 新建项目 | 从模板创建新论文 | - |

## 模板说明

项目提供预设的论文模板，位于 `public/templates/` 目录：

| 模板文件 | 适用领域 | 特点 |
|----------|----------|------|
| `技术类论文.md` | 系统、安全、网络等技术领域 | 包含 Insight 分析、方法设计、实验验证等完整框架 |

模板特点：
- 已包含完整的论文框架结构
- 每个节点都有详细的 `description` 指导
- 示例内容采用系统/安全领域的真实案例
- 可直接基于模板开始写作

## 注意事项

### 数据安全

1. **自动保存**：所有修改会自动保存到浏览器 localStorage
2. **数据备份**：建议定期导出项目文件（.json）进行备份
3. **清除数据**：清除浏览器数据会导致本地项目丢失

### 写作规范

1. **结构保护**：2-5 级标题定义论文框架，请勿随意修改
2. **内容限制**：注意主题句（50字）和支撑句（500字）的字数限制
3. **逻辑连贯**：确保各部分内容前后呼应，逻辑清晰
4. **引用规范**：引用数据和结论时，应注明来源

### 常见问题

**Q: 如何添加新的论点或论据？**
A: 找到 `expandable: true` 的父节点，点击添加按钮创建新的子节点。

**Q: 误删了节点怎么办？**
A: 如果刚删除，可以尝试撤销（Ctrl+Z）；否则需要从备份文件恢复。

**Q: 导出的 Markdown 文件格式是什么？**
A: 导出文件保留完整的标题层级和元数据注释，可用任何 Markdown 编辑器打开。

**Q: 如何在多个设备间同步项目？**
A: 使用"保存项目"导出 .json 文件，在另一设备上"加载项目"导入即可。

